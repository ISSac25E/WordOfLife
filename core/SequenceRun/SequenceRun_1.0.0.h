// SequenceRun
//.h
/*
  Note:
    not to confuse this lib with SequenceBuilder.
    SequenceRun is used to effeciently create and run
    single run sequences(not looping sequences unlike SequenceBuild)
 */
#ifndef SequenceRun_h
#define SequenceRun_h

#include "Arduino.h"

class SequenceRun
{
public:
  void run();
  void seutSequence()

private:
};

class SequenceManage
{
};

//.cpp

#endif
